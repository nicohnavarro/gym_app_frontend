import { IdAlumnoToAlumnoPipe } from './id-alumno-to-alumno.pipe';

describe('IdAlumnoToAlumnoPipe', () => {
  it('create an instance', () => {
    const pipe = new IdAlumnoToAlumnoPipe();
    expect(pipe).toBeTruthy();
  });
});
